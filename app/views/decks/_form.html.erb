<%= form_for(deck) do |f| %>
  <% if deck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deck.errors.count, "error") %> prohibited this deck from being saved:</h2>

      <ul>
      <% deck.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :deckName %>
    <%= f.text_field :deckName, class: "form-control" %>
  </div>

  <div class="field">
    <%= f.label :faction %>
    <%= f.text_field :faction, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit "Save changes", class: "btn btn-primary" %>
  </div>
<% end %>

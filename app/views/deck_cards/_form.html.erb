<%= form_for(deck_card) do |f| %>
  <% if deck_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deck_card.errors.count, "error") %> prohibited this deck_card from being saved:</h2>

      <ul>
      <% deck_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :deckCardId %>
    <%= f.text_field :deckCardId %>
  </div>

  <div class="field">
    <%= f.label :deckId %>
    <%= f.text_field :deckId %>
  </div>

  <div class="field">
    <%= f.label :cardId %>
    <%= f.text_field :cardId %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
